# stock_system_good_us_v2 — 에이전트 지침

## 일일 자동 스캔 루틴: 요약(이메일/푸시) 포맷

스케줄된 일일 스캔 루틴의 마지막 한국어 요약은 사용자의 이메일로 발송되며,
GitHub-flavored Markdown으로 렌더링된다. **표로 표현 가능한 항목은 반드시
마크다운 표로 작성한다.** 구체적으로:

1. **엘리트픽**: 아래 컬럼의 마크다운 표로 작성 (불릿 목록 금지).

   | 순위 | 티커 | 이름 | 가격 | 합산점수 | 급등확률 |
   |---|---|---|---|---|---|

   엘리트픽이 없으면 "엘리트픽 없음"이라고 명시.

2. **전체 후보 TOP 5**: 엘리트픽 표와 겹치지 않는 정보가 있을 때만 별도 표로,
   겹치면 "TOP 5 = 엘리트픽 상위 5개 (티커 나열)" 한 줄로 처리.

3. **tracker 실측 적중률(+10% 터치)**: 집계돼 있으면 보유기간별 표
   (보유기간 10일/20일 / 표본수 / 터치율 / 보유종료수익 평균 / 최대낙폭
   평균)로, 없으면 한 줄로 명시. 주 지표는 20일(스윙 보유 상한) 기준.

4. **실전 코칭 (필수)**: 스캔 리포트의 "[ 실전 코칭 ]" 섹션(또는 스캔
   JSON의 `coaching` 배열)을 기반으로, 1순위 종목(엘리트픽 ∩ 전조패턴)
   별로 아래 표를 반드시 포함한다:

   | 티커 | 매수가 | 목표가(+10% GTC) | 손절가(-7%) | D+3 강홀드↑ | D+3 홀드↑ | D+3 컷↓ |
   |---|---|---|---|---|---|---|

   표 아래에 행동 요령을 날짜와 함께 2~3줄로 쓴다. 예:
   "매수 당일 목표/손절 주문을 같이 걸 것. D+3(날짜) 종가가 강홀드가
   이상이면 최종 적중률 90%로 홀드, 컷가 이하면 손절 전이라도 정리
   (적중률 17%). D+7(날짜)까지 미터치면 D+10(날짜) 전후 정리."
   코칭 대상이 없으면 "코칭 대상 없음(엘리트픽∩전조패턴 공집합)" 명시.

5. 표 외 항목(스캔 기준일, 후보 수, 데이터 수집 실패 종목, 리스크 한 줄)은
   짧은 프로즈/불릿로 유지한다. 리포트 전문은 붙여넣지 않는다.
