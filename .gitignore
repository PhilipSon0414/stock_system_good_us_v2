# ── 민감 정보 (절대 커밋 금지) ──────────────────────────────────
email_config.json

# ── 로컬 생성물 ─────────────────────────────────────────────────
reports/
__pycache__/
*.pyc
.DS_Store

# 주의: v1과 달리 *.json / *.pkl 을 통째로 무시하지 않는다.
# surge_model.pkl, predictions/, tracker_stats.json 은 커밋 대상 —
# 리포지토리만으로 시스템이 재현 가능해야 한다.
